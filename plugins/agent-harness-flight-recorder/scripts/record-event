#!/bin/sh

# Hooks must never interfere with the harness they observe.
command -v python3 >/dev/null 2>&1 || exit 0

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" 2>/dev/null && pwd) || exit 0
python3 "$script_dir/record_event.py" "$@" >/dev/null 2>&1 || :
exit 0
